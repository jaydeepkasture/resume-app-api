# Resume Enhancement API - Architecture Diagram

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT LAYER                             │
│  (Browser, Postman, Python Script, Mobile App, etc.)            │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ HTTP/HTTPS
                             │ JSON Request
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      API LAYER (ASP.NET Core)                    │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  ResumeController                                         │  │
│  │  - POST /api/resume/enhance                              │  │
│  │  - GET  /api/resume/history                              │  │
│  │  - GET  /api/resume/history/{id}                         │  │
│  │                                                            │  │
│  │  [JWT Authentication Middleware]                          │  │
│  │  [Rate Limiting Middleware]                               │  │
│  │  [Exception Handling Middleware]                          │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ Dependency Injection
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    REPOSITORY LAYER                              │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  ResumeRepository (IResumeRepository)                     │  │
│  │                                                            │  │
│  │  - EnhanceResumeAsync()                                   │  │
│  │  - GetUserHistoryAsync()                                  │  │
│  │  - GetHistoryByIdAsync()                                  │  │
│  │                                                            │  │
│  │  [Business Logic]                                         │  │
│  │  [Data Validation]                                        │  │
│  │  [BSON Conversion]                                        │  │
│  └──────────────────────────────────────────────────────────┘  │
└──────────────┬────────────────────────────────┬─────────────────┘
               │                                │
               │                                │
               ▼                                ▼
┌──────────────────────────┐    ┌──────────────────────────────┐
│   INFRASTRUCTURE LAYER   │    │      DATA LAYER              │
│                          │    │                              │
│  ┌────────────────────┐ │    │  ┌────────────────────────┐ │
│  │  OllamaService     │ │    │  │  MongoDB Driver        │ │
│  │  (IOllamaService)  │ │    │  │                        │ │
│  │                    │ │    │  │  Collection:           │ │
│  │  - BuildPrompt()   │ │    │  │  resume_enhancement_   │ │
│  │  - CallOllama()    │ │    │  │  history               │ │
│  │  - ParseResponse() │ │    │  │                        │ │
│  └────────────────────┘ │    │  │  Indexes:              │ │
│                          │    │  │  - user_id             │ │
└──────────┬───────────────┘    │  │  - user_id + created_at│ │
           │                    │  └────────────────────────┘ │
           │ HTTP POST          └──────────────────────────────┘
           ▼
┌──────────────────────────┐
│   EXTERNAL SERVICE       │
│                          │
│  ┌────────────────────┐ │
│  │  Ollama API        │ │
│  │  localhost:11434   │ │
│  │                    │ │
│  │  Model:            │ │
│  │  llama3.1:8b       │ │
│  │                    │ │
│  │  Endpoint:         │ │
│  │  /api/generate     │ │
│  └────────────────────┘ │
└──────────────────────────┘
```

## Request Flow Diagram

```
┌──────────┐
│  Client  │
└────┬─────┘
     │
     │ 1. POST /api/resume/enhance
     │    Authorization: Bearer {JWT}
     │    Body: { resumeData, enhancementInstruction }
     ▼
┌─────────────────┐
│ ResumeController│
└────┬────────────┘
     │
     │ 2. Validate JWT & Extract UserId
     │
     ▼
┌──────────────────┐
│ ResumeRepository │
└────┬─────────────┘
     │
     │ 3. Call OllamaService.EnhanceResumeAsync()
     │
     ▼
┌──────────────┐
│ OllamaService│
└────┬─────────┘
     │
     │ 4. Build Prompt with Resume + Instruction
     │
     │ 5. HTTP POST to Ollama API
     │    { model, prompt, stream: false }
     ▼
┌──────────────┐
│  Ollama LLM  │
│  (Local AI)  │
└────┬─────────┘
     │
     │ 6. Process with llama3.1:8b
     │    Generate Enhanced Resume
     │
     │ 7. Return JSON Response
     ▼
┌──────────────┐
│ OllamaService│
└────┬─────────┘
     │
     │ 8. Parse JSON from LLM Response
     │    Extract Enhanced Resume
     │
     ▼
┌──────────────────┐
│ ResumeRepository │
└────┬─────────────┘
     │
     │ 9. Convert to BSON Documents
     │
     │ 10. Save to MongoDB
     │     - original_resume
     │     - enhanced_resume
     │     - enhancement_instruction
     │     - user_id
     │     - created_at
     │     - processing_time_ms
     ▼
┌──────────┐
│ MongoDB  │
└────┬─────┘
     │
     │ 11. Return Success + History ID
     ▼
┌──────────────────┐
│ ResumeRepository │
└────┬─────────────┘
     │
     │ 12. Build Response DTO
     ▼
┌─────────────────┐
│ ResumeController│
└────┬────────────┘
     │
     │ 13. Return HTTP 200 OK
     │     {
     │       status: true,
     │       message: "Enhanced in 3542ms",
     │       data: {
     │         originalResume,
     │         enhancedResume,
     │         historyId,
     │         processedAt
     │       }
     │     }
     ▼
┌──────────┐
│  Client  │
└──────────┘
```

This architecture provides a **scalable, maintainable, and secure** solution for AI-powered resume enhancement! 🚀
